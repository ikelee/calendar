name: Trigger Scraper

on:
  schedule:
    - cron: '*/10 * * * *'  # Run every 10 minutes
  workflow_dispatch:  # Allow manual triggering

jobs:
  trigger:
    runs-on: ubuntu-latest
    
    steps:
    - name: Trigger scraper
      run: |
        curl -X POST ${{ secrets.API_URL }}/trigger-scraper
      env:
        API_URL: ${{ secrets.API_URL }} 